$(document).ready(
	function () {
		
	$('.datetimepicker5').datetimepicker({
		format: "YYYY/MM/DD"
		//format: "H:i"
	});
		$('.datetimepicker6').datetimepicker({
		format: "YYYY/MM/DD"
		});

		
		
});